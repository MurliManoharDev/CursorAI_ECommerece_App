export const environment = {
  production: true,
  // Use relative path when Angular and API are hosted on the same domain
  apiUrl: '/api'
  
  // If you need to use absolute URL (e.g., API is on a different domain), uncomment below:
  // apiUrl: 'https://cts-vibeappau2812-1.azurewebsites.net/api'
}; 